info:
  name: Signup
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "id": "a5113bcc-976a-4f5b-b1f0-7e4d39d6f376",
        "email": "nuevoemail123@email.com",
        "password": "Password1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
