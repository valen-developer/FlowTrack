info:
  name: Get User Logged Workspaces
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/work/workspace"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
