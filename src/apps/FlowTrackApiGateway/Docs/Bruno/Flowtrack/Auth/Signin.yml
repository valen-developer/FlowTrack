info:
  name: Signin
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/iam/auth/signin"
  body:
    type: json
    data: |-
      {
        "email": "email@email.com",
        "password": "Password1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
